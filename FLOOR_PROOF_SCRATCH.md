# Temporary: floor check 7 verification

This file exists only to prove `doc-link-check` actually fails. The link
below points at a file that does not exist, so lychee's `--offline` mode
must resolve it against the checkout and report it dead.

See [a file that does not exist](./DOES_NOT_EXIST_ANYWHERE.md).

It is removed in the very next commit.

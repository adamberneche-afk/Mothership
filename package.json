{
  "name": "ai-cto-hub",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "for f in scripts/dev-test-*.mjs; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1"
  }
}

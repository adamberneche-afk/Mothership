# Local Lessons Learned

- [Initial Setup]: Hub initialized with basic autonomous agent and workflows.
- [2026-08-05]: Discovered the autonomous agent never fetched real code from spoke repos - it only read lessons.md/NORTH_STAR.md and then asked an LLM to audit "the latest code" with none actually in the prompt. This produced ~2,000 hallucinated GitHub issues in one spoke (tso) over ~4 months. Fixed by fetching the spoke's latest commit diff and including it in the prompt, and by skipping the AI call entirely when there's no usable diff.
- [2026-08-05]: The AI's JSON response was trusted blindly - a parse failure fabricated a synthetic result and posted an issue anyway, and a successful-but-wrong-shaped response got string-interpolated straight into the issue body (producing literal "undefined" / "[object Object]"). Fixed by validating the response shape before ever calling `issues.create`, and skipping (not fabricating) on any failure.
- [2026-08-06]: Fixing the hallucination root cause wasn't enough on its own to trust re-enabling automation - a bug in a future prompt/model change could still refile the same mistake at scale before anyone noticed. Added two independent safety rails on top of the validation fix: a `DRY_RUN_MODE` env var (default on) that reports findings without ever filing them, and a hard `RATE_CAP_PER_REPO_PER_DAY` (default 3) once dry-run is off. Both fail safe - a missing/misconfigured env var means no issue gets filed, not the reverse.
- [2026-08-06]: `ai_decision_log.json` was part of the spoke setup contract from the start but nothing ever read or wrote it - real decision logging is now live. Every decision (skip, dry-run finding, rate-capped, created) gets appended with its commit sha, so the same commit+mode combination is never re-analyzed by the AI twice, and the last few decisions get fed back into the prompt so the model has memory of what it already decided instead of re-litigating it every run.
- [2026-08-06]: The "Recursive Learning Loop" the README described was aspirational - nothing aggregated insights across spokes automatically. Built `api/recursive_learning.js`: a monthly job that reads every spoke registered in `spokes.json`, looks for a pattern that recurs across more than one of them, and - if it finds one - opens a PR against this repo proposing an update to `universal_lessons.md`/`north_star_framework.md`. It never pushes directly to `main`; a human still reviews and merges. Seeded `spokes.json` with `tso`, the one spoke actually connected today.
- [2026-08-06]: `scripts/prune-logs.js` had been a no-op stub since the hub's first setup - "Pruner initialized..." and nothing else, never invoked by any workflow. Replaced with a real implementation: for every spoke in `spokes.json`, archive `ai_decision_log.json` entries older than a retention window into `ai_decision_log_archive.json` (archive written before the live log is truncated, so a mid-run failure can't lose data), then wired it into a weekly Sunday workflow. Runs as a plain Actions script, not a Vercel function - no AI involved, so it only needs a GitHub token, not the AI credentials.
- [2026-08-06]: `setup_spoke.py`'s generated `call-hub.yml` had two real problems: it only ever computed `debug`/`refactor` from an inline workflow expression (never `hunt`, despite the hub handler supporting it), and it baked the hub's URL directly into the curl command at generation time - completely ignoring the `VERCEL_URL` secret the README told operators to set. Fixed both: added a third cron for `hunt`, replaced the inline expression with a step-output `case` statement (matches the existing "prefer step-outputs over inline expressions" lesson in the hub's own `hub_lessons.md`), and switched the generated workflow to actually read `VERCEL_URL` (plus `VERCEL_BYPASS_TOKEN`) from secrets like the docs always claimed it did.
- [2026-08-06]: Only `tso` was ever actually wired to this hub, and even that was ad hoc - its workflow hardcoded the hub's URL directly rather than using the `VERCEL_URL` secret convention (that PR is up on the `tso` repo). `thinkos-server` and `tais` had zero integration at all - no workflow, no spoke contract files. Onboarded both via `setup_spoke.py`'s output (with real, project-specific `NORTH_STAR.md` content pulled from each repo's own README, not left as an unfilled template), and registered all three spokes in this repo's new `spokes.json`. Safe to leave the schedule enabled on the two new spokes immediately, since the hub's `DRY_RUN_MODE` defaults to on hub-wide - nothing files an issue until that's verified and flipped.
- [2026-08-06]: The `health-report.yml` this repo used to have was a non-functional copy-paste of `tso`'s own health tooling - it referenced `scripts/health-analyzer.js` and Prisma, neither of which exist here, and posted comments literally saying "Auto-generated by TSO Health System". Built a real, Mothership-native replacement (`scripts/health-report.js`): reports on the hub/swarm's own health per registered spoke (issues filed, decisions logged, skip rate, inferred live/dry-run status), published as a single pinned issue on this repo that gets updated in place rather than spammed on every run.
- [2026-08-07]: A debugging pass over the just-shipped sprints found four real gaps: `scripts/prune-logs.js` had no retry on a stale-`sha` write conflict (unlike the decision-log appender it shares a file with) - fixed by retrying the whole read-partition-write cycle. `scripts/health-report.js` never reopened the pinned report issue if a human closed it - future runs would silently keep rewriting a closed issue's body. `api/recursive_learning.js` hardcoded `main` as the base branch instead of looking up the repo's actual default branch. `api/autonomous_agent.js`'s "already decided" replay only surfaced a prior `created` decision's `issueUrl` nested under `priorDecision`, not at the top level where a live response puts it. All four fixed with test coverage.
- [2026-08-07]: The retry fix above for `scripts/prune-logs.js` introduced a subtler bug of its own: the retry loop re-reads and re-partitions the live log from scratch on every attempt, but the archive write inside that loop just appended `[...archiveEntries, ...old]` unconditionally. If the *archive* write on one attempt succeeded but the *live-log truncation* write right after it then failed (the exact scenario the retry exists to handle), the next attempt recomputed the same `old` set from the still-untruncated live log and appended it to the archive a second time - real, in-archive duplicate entries from a single invocation, not the "safe, idempotent on the next run" duplication the archive-then-truncate ordering is meant to allow for across separate runs. Fixed by deduping against already-archived entries (by timestamp+commitSha+mode) before writing, and skipping the archive write entirely when there's nothing new to add. Reproduced the bug in a standalone script before fixing it, and added a regression test to `dev-test-prune-logs.mjs` that asserts the archive ends up with exactly the moved entries, not doubled.
- [2026-08-07]: `setup_hub.py` - the installer this repo has carried since before any of the above sprints, meant to scaffold a brand-new hub from scratch - never generated any of the four `.github/workflows/*.yml` files (`self-reflect`, `prune-logs`, `health-report`, `recursive-learning`). Someone running it fresh would get `api/*.js` and `scripts/*.js` deployed to Vercel and committed, and nothing would ever actually invoke them - a hub that deploys cleanly and then sits completely inert, with no error anywhere to say so. This is the same class of gap `DOCS_VS_CODEBASE.md` exists to catch, just one level down: the installer's own claim (that running it produces a working hub) didn't match what it actually produced. Fixed by adding all four workflow files to its generated file list, embedded and kept in sync the same way `api/autonomous_agent.js` etc. already were, verified by diffing a scratch-dir install's output against the real files. Also caught `.gitignore`'s embedded copy had drifted (missing `.vercel`/`__pycache__/`/`*.pyc`, added to the real file at some point after this script was first written) - synced that too.
- [2026-08-07]: Every fix above this line was verified locally (`node scripts/dev-test-*.mjs`, `python3 -m py_compile setup_hub.py setup_spoke.py`) before it shipped - but nothing ran that sweep automatically. It only happened because whoever was making the change remembered to run it by hand, every time, after every merge. Half the defects logged above were sitting in code that already had a passing test - proof that "it passed its tests" only helps if something actually re-runs those tests on every future change, not just the one where they were written. Added `.github/workflows/ci.yml`: runs the full local sweep (all four `dev-test-*.mjs` harnesses, then `py_compile` on both installer scripts) on every pull request, on every push to `main`, and on demand. Needs zero secrets - every harness already runs against a fully mocked `octokit`/`fetch` - so it's safe to run even on a PR from a fork.
- [2026-08-08]: Finished the two things the CI workflow above deliberately left out, plus a third bug found while doing it. First, a `package.json` `"test"` script (`for f in scripts/dev-test-*.mjs; do node "$f" || exit 1; done`), with `setup_hub.py`'s embedded copy re-synced to match - CI now runs `npm test` instead of duplicating that loop inline. Second, a CI step that actually runs `setup_hub.py` into a scratch directory and diffs its output against this repo's real files (`api/*.js`, `scripts/*.js`, all four workflow YAMLs, `package.json`, `.gitignore` - deliberately excluding `spokes.json`/`universal_lessons.md`/`north_star_framework.md`, which are meant to diverge from this repo's live state), so a repeat of PR #13's bug (missing workflows in the generated output) would now fail CI automatically instead of waiting for someone to notice by hand. Third, found while writing that scratch-dir step: `setup_hub.py`'s own dependency install, `subprocess.run(["npm", "install"], shell=True)`, never actually ran `npm install` - combining `shell=True` with a list means everything after the first item becomes an argument to the *shell*, not to `npm`, so it silently ran bare `npm` (prints usage, does nothing) on every install, for as long as this script has existed. `node_modules` was never created; anyone who actually ran this installer got a hub with no `@octokit/rest` and no error saying why. Fixed by dropping `shell=True` (a plain list already execs correctly without a shell) and warning, not crashing, if `npm install` still fails for some other reason.
- [2026-08-08]: Every blocker left on this project traced back to one thing: the hub's Vercel deployment sits behind Deployment Protection, and getting a bypass token needs dashboard access nobody here has. Rather than keep waiting on that, added a full alternative hosting path: **`gas/`**, a Google Apps Script port of `api/autonomous_agent.js`/`api/recursive_learning.js`, deployed as a Web App - which has no deployment-protection-style wall to fight, since "Anyone" access is a normal, supported option. Same decision logic, same validation/dry-run/rate-cap rails, same decision-log dedup; three real platform differences accounted for: no `@octokit/rest` (`gas/github.js` is a small hand-rolled REST client on an injected HTTP primitive instead), no Node `fetch`/Promises (Apps Script's `UrlFetchApp` is synchronous, so this code is fully synchronous too, matching the platform instead of fighting it), and no local filesystem (the hub's own `universal_lessons.md`/`north_star_framework.md`/`hub_lessons.md` are fetched from this repo via the GitHub API instead of read off disk - arguably an improvement, since it means no redeploy is needed to pick up an edit to those files). `api/*.js` and the Vercel path are untouched and stay in the repo; dropping Vercel entirely is a deliberate future step once Apps Script is actually verified live, not bundled into adding the alternative.
- [2026-08-08]: Apps Script has no `import`/`export` - every file in a project shares one global function scope, closer to classic script concatenation than real ES modules. Learned this the concrete way: `gas/autonomous_agent.js` and `gas/recursive_learning.js` both declared `const DEFAULT_HUB_OWNER`/`DEFAULT_HUB_REPO`, which is completely harmless as two separate ES modules but is a real `SyntaxError` the instant both files share one Apps Script project's scope. Caught by the test harness itself - `scripts/gas-test-harness.mjs` loads the literal, unmodified `gas/*.js` source through Node's `vm` module the same way Apps Script combines files, specifically so what's tested is byte-for-byte what `clasp push` uploads, no bundler or compiled intermediate in between. Fixed by hoisting both constants into a new `gas/constants.js`, declared exactly once, loaded before anything that references them.
- [2026-08-08]: Also built a standalone **PWA dashboard** (`dashboard/`) for watching spoke health without waiting on the weekly pinned issue - a separate, additive piece, not a replacement for anything (a PWA can't receive GitHub Actions' POST mid-cron-job, so it was never a candidate to replace the Vercel/Apps Script backend). Since Mothership and every spoke are public repos, `dashboard/app.js` reads `spokes.json` and each spoke's `ai_decision_log.json`/issue list straight from `api.github.com`, client-side, computing the exact same metrics `scripts/health-report.js`'s `buildReportForSpoke` already does - no backend of its own, installable via `manifest.json`+`sw.js`, hostable for free on GitHub Pages. Verified the approach against real live data (not just by inspection): `spokes.json` fetched and decoded correctly; the issues-list call then hit GitHub's unauthenticated 60-requests/hour cap on this environment's shared IP, which is the exact limitation already disclosed in the dashboard's own footer and `app.js`'s header comment - confirmed the graceful-degradation path (`null` -> rendered as "n/a") is what actually fires, not a crash.
- [2026-08-09]: Asked directly whether the docs were current, so actually checked instead of assuming: `README.md` never mentioned the PWA dashboard anywhere, not even in this same PR that added it, and the "Global Cost Management" bullet still said AI traffic flows through "a single Vercel deployment" - true when it was written, stale the moment the Apps Script alternative existed in the same PR. Both fixed here: a new "Health Dashboard" subsection, and the Overview bullet now names both deployment options. `README.md`'s own "no CI" gap (added by an earlier PR, unrelated to this one) is being fixed separately against `main`, not bundled into this PR's already-large diff.
- [2026-08-10]: A UI/UX audit of `dashboard/` (the one real user-facing surface in this whole project) found 20 concrete issues, verified by actually loading the page in a real headless browser against mocked and live `fetch()` responses rather than just reading the code. The most consequential: `fetchDecodedFile`/`countIssuesCreatedSince` collapsed every failure - a genuine outage, a 404 on a file that doesn't exist yet, and hitting the disclosed 60-req/hr rate cap - into the same bare `null`, which then rendered as the same **critical/red** "no decisions logged" chip as an actual problem. Hitting the rate limit (the single most likely real failure mode, per the dashboard's own footer) painted every spoke red, indistinguishable from a real outage. Fixed by propagating a real `{ ok, rateLimited }` result through the fetch helpers instead of a bare `null`, giving "rate-limited," "couldn't load," and "no decisions logged this window" three distinct, correctly-colored states (warn/critical/neutral respectively) - confirmed via a mocked-`fetch` browser test asserting each renders its own chip class, not just by reading the code. Also fixed a real race condition (clicking Refresh twice while a load was in flight produced duplicated rows - confirmed by reproducing it, then confirming a disabled-button guard closes it), parallelized what was a serial per-spoke fetch loop, added severity-based row sorting, mapped raw `outcome` enum values to human labels, and a batch of accessibility/PWA fixes (`aria-live` on the status line, focus-visible styles, a table caption, "opens in new tab" hints, a `color-mix()` fallback background, service-worker `skipWaiting`/`clients.claim` plus an install-failure `.catch` and an offline fallback response, and `apple-touch-icon`/manifest PNG + maskable icons rendered from `icon.svg` via headless Chromium - `rsvg-convert`/ImageMagick/`sharp` were unavailable in this environment, so Playwright's pre-installed browser did the SVG→PNG conversion instead). One thing learned along the way while building the rate-limit detection: a cross-origin `fetch()` to `api.github.com` can't reliably read `x-ratelimit-remaining` (browsers only expose CORS-safelisted response headers to JS unless the server adds `Access-Control-Expose-Headers`) - confirmed by a mocked-header test that initially failed silently - so the fix treats any 403 from this API surface as a rate limit (every endpoint this dashboard calls 404s on a missing resource, not 403) rather than depending on a header that may not be visible at all.
- [2026-08-09]: Asked directly whether the docs were current - checked instead of assuming, and found two real gaps on `main` itself, independent of anything still sitting in an open PR. `README.md` never once mentioned the CI workflow, the four test harnesses, or `npm test`, despite CI enforcing all of it on every PR since #14 - added a "Testing & CI" section describing what runs and why it exists. `DOCS_VS_CODEBASE.md`'s update banner still said "PRs #3–#10" and made no mention of the three debugging passes or the CI work that came after - updated the banner and capability #7's row to reflect the current PR range and note that a tested (if not yet merged) alternative to the Vercel blocker exists.
- [2026-08-09]: Ran the same "is this current" check again after a new `docs-check` CI job landed on its own branch (independent of this one). Found the `## Testing & CI` section this PR adds describes `ci.yml` as running the test suite and the installer-drift check only - it doesn't mention `docs-check`, since that job was branched off `main` after this PR already existed and never touches `README.md` itself (correctly so, by its own design - it only *modifies* the already-existing `ci.yml`, which isn't a new file under a watched directory). That's a real, disclosed blind spot in `docs-check`'s own design (modifications to already-documented files aren't watched) caught here by the same manual-check discipline it exists to replace for the *other* failure mode. Added one sentence naming the `docs-check` job so this section stays accurate once both PRs land, whichever merges first.
- [2026-08-09]: Twice this session a PR added a whole new capability surface (`.github/workflows/ci.yml`; `dashboard/`, in the same diff as the PR that introduced it) with zero mention in `README.md` - both times only caught later by someone directly asking "is this current?" and diffing docs against code by hand. That's the same class of gap this whole project exists to fix, just recurring at the documentation layer instead of the code layer. Added a `docs-check` job to `.github/workflows/ci.yml`, same shape as the existing installer-drift check: it looks specifically at **new** files (`git diff --diff-filter=A`, not just any change) added under `api/`, `gas/`, `dashboard/`, or `.github/workflows/` - a brand-new capability arriving with no doc mention at all is the actual failure mode that happened twice; a bug fix inside an already-documented file isn't. If any such file was added, `README.md` must also appear in the PR's changed-file list, or the job fails with `::error::` pointing at exactly which new file(s) are undocumented. A `[skip-docs-check]` marker in the PR title/body is a deliberate escape hatch for genuine non-capability additions (an internal helper, a test fixture) without needing a token README edit just to satisfy CI. Verified the pass/fail logic against four synthetic commit scenarios locally (new watched file + no README ⇒ fail; new watched file + README ⇒ pass; modifying an existing watched file ⇒ pass, since it's not a new file; a new file in an unwatched directory like `scripts/` ⇒ pass) before pushing.
- [2026-08-10]: A second UX pass over `dashboard/`, this time hunting specifically for "invisible until it matters" gaps rather than visible bugs (the first pass already fixed those). The most consequential: a single malformed `spokes.json` entry (a stray `null`, or an object missing `owner`/`repo`) used to throw inside `buildReportForSpoke`, and the old `Promise.all` fan-out rejected for *every* spoke on that one bad entry with no `.catch` anywhere - the whole dashboard silently wedged on "Loading…" forever. Fixed by filtering malformed entries out at parse time and switching the per-spoke fan-out to `Promise.allSettled`-shaped isolation (each spoke's own try/catch, `safeBuildReport`) so one spoke's surprise is that spoke's problem, never everyone else's - and as a direct side effect, rows now stream in as each spoke resolves instead of all waiting on the slowest one. Also added: a 15s `AbortController` timeout on every fetch (a hung request used to leave the Refresh button stuck disabled forever, with no recovery short of a hard reload); a 45s `sessionStorage` cache with stale-while-revalidate (repaint instantly from cache, "(cached)" labeled, while a background refetch patches the row in place - guarded against private-browsing/quota errors, and deliberately never caches a failure status, only real successful reports) given the dashboard's own disclosed 60-req/hr cap; a keyed row-reconciliation renderer (`renderRows`/`updateRow`) replacing the old clear-and-rebuild, so a manual Refresh no longer blanks the table before repopulating it - existing rows just update or get patched in place; a `document.title` marker for backgrounded/pinned tabs when a spoke is in a genuine failure state (deliberately NOT for the benign, disclosed rate-limited state - only a real "couldn't load"/"timed out" should make a background tab worth checking); and a live "updated Xs/Xm ago" relative timestamp instead of a frozen absolute one. Verified every one of these against the real page in headless Chromium with mocked routes, not just by reading the code - including a scratch copy with a shortened timeout constant specifically to prove the `AbortController` path fires and recovers the UI without waiting out the real 15s in the test itself.
- [2026-08-10]: A third audit of `dashboard/` (told explicitly to be honest rather than pad the list if little was genuinely left) caught something real in the *second* pass's own not-yet-merged work: the sessionStorage cache never actually skipped a network call. `loadDashboard`'s per-spoke loop read the cache to paint an instant placeholder, then unconditionally still called the real 2-requests-per-spoke fetch regardless of whether that cache was fresh - directly contradicting the code's own comments (and this file's prior entry above) claiming it "avoids paying for the same answer twice." The second pass's own verification test happened to assert this buggy behavior as correct (it checked a revalidation *did* happen on a second load within the TTL window, without checking whether it *should* have been skipped) - passing tests proving the wrong thing, the same class of trap this whole file exists to catch. Fixed by adding a `force` parameter to `loadDashboard`: passive triggers (initial load, and two new ones below) skip the network entirely when a spoke's cache is still within `CACHE_TTL_MS`, while an explicit user action (Refresh click, `'r'` keypress) always forces a real fetch regardless of freshness - a user who just asked for current data shouldn't silently get served something up to 45s old just because the cache's own clock calls it "fresh enough." Verified directly by counting mocked-route hits (not just watching the screen): a passive re-check within the TTL window adds zero requests; an explicit Refresh within that same window still adds a full fresh fetch; and after the TTL genuinely expires (tested via a scratch copy with `CACHE_TTL_MS` temporarily shortened, same technique as the prior timeout test), a passive re-check does refetch. On top of the fix: a `visibilitychange` listener refetches on return-to-tab (deliberately unforced, so it only spends anything on what's actually stale per spoke - a blind polling interval was considered and rejected, since its safe cadence shrinks every time `spokes.json` grows a spoke, while a cache-gated visibility trigger doesn't need re-tuning); an `online` listener self-heals the moment connectivity returns instead of waiting for a manual click; a `<link rel="preconnect">` to `api.github.com` so DNS+TLS setup starts before the first `fetch()` call rather than at it; and a live "retry in Xm/Xs" countdown for the rate-limited status (reusing the existing 15s render tick) instead of a frozen absolute retry time. Explicitly declined one item the audit raised and then talked itself out of: per-row screen-reader announcements as cached rows get patched with fresh data - the existing `#status` live region already brackets the whole load with a clear start/end announcement, and per-row chatter as background fetches resolve underneath a screen-reader user would be more disruptive than helpful.
- [2026-08-10]: Asked how to limit hosting requirements and maximize functionality without required connections - researched both halves of the system rather than guessing. **Backend:** investigated whether `autonomous_agent.js`/`recursive_learning.js` could run as a plain GitHub Actions script (zero external hosting at all), triggered by a spoke firing `repository_dispatch` instead of POSTing a URL - confirmed this is technically *less* migration work than the `gas/` port already completed (`processRequest`/`runRecursiveLearning` already read config straight from `process.env`, and a Node Actions runner already has native `fetch`, unlike Apps Script). But it has a real cost: every spoke would need its own PAT with write access to the Mothership repo just to fire the dispatch - a brand-new credential type spokes never hold today (they currently POST a plain URL, no credential at all), and that's a real new attack-surface increase that scales with spoke count. Presented the trade-off; decided to keep the existing hosted-URL model (Apps Script) rather than take it on - `gas/` already fully solves "limit hosting requirements" for the backend (no GCP billing, no Vercel-style Deployment Protection wall to fight) without introducing any new credential-distribution risk. Not building the Actions-only path; noting the decision here so it isn't silently re-litigated later without remembering why. **Dashboard:** confirmed via a real headless-browser test (not just reasoning, and bypassing this sandbox's own TLS-proxy artifact with `ignoreHTTPSErrors: true` to see genuine behavior) that opening `dashboard/index.html` directly as a `file://` URL - true zero-hosting - does not work for live data: GitHub's API rejects the `file://` page's `null` origin via CORS (`No 'Access-Control-Allow-Origin' header is present`), and the service worker refuses to register under `file://` at all (`file://` isn't a "secure context"). This is a hard, confirmed platform constraint, not something fixable client-side. The real, useful finding: the bar is still much lower than "use GitHub Pages" implies - any plain static HTTP(S) server clears it, including `python3 -m http.server` on your own machine. Documented this precisely in `README.md` instead of only ever mentioning GitHub Pages. Separately, added real durable offline capability: a `localStorage` "last known good" layer (no expiry, survives across browser sessions - distinct from the existing 45s `sessionStorage` cache, which exists only to cut request volume and deliberately forgets everything when the tab closes) that a genuinely offline load - a brand-new tab, zero network, possibly days later - falls back to instead of a bare failure message, for both the per-spoke data *and* the `spokes.json` registry itself (missing the latter would have meant the whole per-spoke fallback never even ran, since a failed `spokes.json` fetch returns early before reaching the per-spoke loop - caught this during design, not after). Rendered with an explicit "(offline - last seen 3d ago)" label and forced to a neutral chip color regardless of the historical status, so a days-old "live" can never misread as "currently fine" at a glance. Verified end-to-end with real browser sessions sharing `localStorage` across separate tabs (one online tab writes the fallback data, a second brand-new offline tab reads it back correctly, including when `spokes.json` itself also fails), a never-before-seen spoke correctly falling through to the plain failure message with nothing to recover, and a simulated private-browsing-style storage failure (`localStorage` throwing on every access) degrading to "no offline fallback" without crashing anything.
- [2026-08-11]: Asked directly whether the docs were current, so checked instead of assuming - found a real gap on `main`. The `docs-check` CI job (added in #17) is scoped, on purpose, to catch a *new* undocumented capability file, not an existing capability whose docs fell behind further work on it - and that's exactly what happened here: PR #18's whole second/third UX pass (fetch timeouts and the "timed out" status, malformed-`spokes.json`-entry filtering, per-spoke fault isolation and streaming rows, the `visibilitychange`/`online` auto-refetch listeners, the `document.title` failure marker, the live "Updated .../retry in ..." countdowns, the `r` keyboard shortcut) only ever touched already-existing `dashboard/app.js`/`index.html`, so the check correctly never fired - and nothing else caught that README's Health Dashboard section still only described the original design plus PR #19's cache/offline-fallback work, with none of PR #18's own additions ever written down. Added two new paragraphs ("Resilience," "Staying current without asking") covering what PR #18 actually shipped. Also bumped `DOCS_VS_CODEBASE.md`'s "last checked" date - its content itself was still accurate (capability #7 correctly still reads "verified locally only, no real `clasp` deployment... yet," reconfirmed true again this session when asked to actually deploy it and finding no Google/Apps-Script credential available to do so from here).
- [2026-08-12]: Asked whether the codebase reflects the new "Why This Exists" value proposition - checked live via the GitHub API instead of assuming "not deployed yet" was the whole story, and found it wasn't. Every mechanism the section describes is real, tested code (confirmed line-by-line against `api/autonomous_agent.js`, `api/recursive_learning.js`, `scripts/health-report.js`, `dashboard/`) - but two of the operator-facing claims in it are more broken than "not yet live": `recursive-learning.yml` has **zero runs, ever**, despite being merged and scheduled monthly since Sprint 2 - the cross-spoke learning loop has never executed once, not even a dry run. `health-report.yml` has run 5 times and **failed all 5**, most recently with `401 Bad credentials` calling `api.github.com` - the `GLOBAL_GITHUB_TOKEN` Actions secret on this repo is set to something invalid, not merely unset, so the "single pinned health-report issue" claim is actively erroring today, not pending a future deployment step. Separately, `self-reflect.yml` shows 17/20 runs "successful," which is easy to misread as evidence of live function - it almost certainly just means "curled Vercel's 403 Deployment Protection response and exited 0," since the workflow doesn't hard-fail on a non-2xx; a green checkmark here isn't proof of a real hub response, and future status reports should say so explicitly rather than citing it as-is. Also found, unrelated to any deployment blocker and fully fixable in-repo: `thinkos-server` and `tais`'s `call-hub.yml` still carry their original `schedule:` triggers and have been failing *every single scheduled run* (102 and 135 runs respectively, 0 successes on either) since the day they were onboarded, because `VERCEL_URL`/`VERCEL_BYPASS_TOKEN` are empty secrets and `curl` fails on URL parsing before any network call is attempted. `tso` hit this same class of problem after its own incident and disabled its schedule (`workflow_dispatch`-only, with a comment explaining why); `thinkos-server`/`tais` never got that treatment and have been silently spamming failed-run notifications ever since. Fixed by applying the same schedule-disable pattern to both (see `call-hub.yml` on each repo) - written as its own, accurate comment, since their root cause (missing secrets) isn't `tso`'s root cause (no diff context in the prompt, since fixed). `README.md`'s "Why This Exists" also got one added clause disclosing that its operator-UX paragraph describes designed/tested behavior, not something live today. The `GLOBAL_GITHUB_TOKEN` 401 itself is not fixed here - no tool available to this session can create or rotate a repo secret; flagging it plainly instead of working around it.
- [2026-08-12]: Asked to review `north_star_framework.md`/the value prop and propose features that would strengthen it - picked "Hub self-health panel" from the resulting menu, since it maps directly onto a real, already-confirmed gap: `health-report.yml` has failed all 5 of its runs and `recursive-learning.yml` has never run once, and nothing today would tell an operator that short of the same forensic Actions-API digging already done this session, directly undermining paragraph 3 of the value prop ("glanceable confidence, not a reason to dig through logs"). Added a second panel to `dashboard/` showing whether Mothership's own scheduled workflows (`self-reflect`, `health-report`, `recursive-learning`, `prune-logs` - `ci.yml` deliberately excluded, it's a PR/push gate not an operational signal) are actually succeeding, reading GitHub's Actions run-history API (`/actions/workflows/{file}/runs`) the same unauthenticated way `app.js` already reads issues/contents. Mirrors the existing per-spoke pattern closely rather than inventing a new one: same cache-first/stale-while-revalidate/force-bypass shape, same worst-first severity ranking and chip classes, same keyed-reconciliation row rendering - `loadDashboard` split into independent `loadSpokeSection`/`loadHubHealthSection` functions run via `Promise.allSettled` so one section's failure (or its own early-return branches) never blocks the other, matching the same fault-isolation principle already applied per-spoke. **A real, disclosed unknown, not silently assumed away:** this feature's "no new backend" premise depends on the Actions API being unauthenticated-readable and CORS-enabled for public repos the same as Issues/Contents - tried to reconfirm this live the same way a prior pass confirmed the file:// CORS behavior (real headless Chromium making its own `fetch()` calls), but this session's *current* network egress blocks even that prior pass's own previously-working baseline call, so nothing could be re-verified against the real `api.github.com` this time, only against mocked routes. Documented API behavior points toward this working; built so a wrong guess fails safe regardless (`fetchJson`'s existing error handling already renders any non-2xx as a clearly-labeled "couldn't load" row, never a crash) - and said so plainly rather than claiming more certainty than actually exists. Verified via a real local static server + real headless Chromium with mocked `page.route()` responses (11 checks): all five hub-workflow statuses render with the correct chip/severity (`healthy`/`failing`/`never run`/`rate-limited`, worst-first ordering); the `document.title` ⚠ marker fires for a failing hub workflow even when every spoke is healthy, and clears when both sections are clean; caching genuinely skips the network on a passive re-load and a forced Refresh always bypasses it (counted actual mocked-route hits, not just watched the screen); and a hard `spokes.json` failure's error message survives the hub-health section's own independent success instead of being overwritten by a generic timestamp.
- [2026-08-12]: Asked whether credentials could be pulled into the Apps Script deployment more conveniently. Clarified first, since the phrase "inside that dashboard" was ambiguous between the public `dashboard/` PWA (which would have been a real mistake - it's unauthenticated, client-side JS meant to be served to anyone, so any secret placed there would be readable by any visitor) and a settings page served by the Apps Script Web App itself, which is what was actually meant. Built the latter: `gas/settings.js` adds a token-gated page (`?endpoint=settings&token=...`, new `doGet` in `gas/Code.js` - there was no `doGet` at all before this) for viewing/updating the deployment's config without reopening the Apps Script IDE each time. This can't remove the one manual bootstrap step - `appsscript.json`'s webapp access is `ANYONE` (required so spokes with no Google credential can still call the JSON endpoints), so a new `doGet` needs its own gate, and that gate is necessarily one shared secret (`ADMIN_SETTINGS_TOKEN`) still set the old way, via Script Properties, first. Once bootstrapped, the other 8 values can be rotated through the page instead. Security choices made deliberately, not incidentally: `ADMIN_SETTINGS_TOKEN` itself is never rendered or writable through this page (rotating it always requires the IDE, so a leaked page token can't mint its own replacement); a wrong or missing token gets the same generic response either way, so probing the URL doesn't confirm the feature exists; secret fields (`AI_API_KEY`, `GLOBAL_GITHUB_TOKEN`) render masked and start blank, so leaving one untouched and saving means "keep the current value," never "clear it"; and `saveSettings()` writes through an explicit allowlist of the 8 config keys rather than a pass-through of whatever the client sends, so `ADMIN_SETTINGS_TOKEN` can never end up in a write even if a caller tries to smuggle it into the payload. Verified via the same Node `vm`-based harness as every other `gas/` file (`scripts/dev-test-gas-settings.mjs`, extending the fake `PropertiesService` to actually record `setProperties` calls instead of read-only): confirmed the bootstrap-needed and wrong-token pages never contain any real config value or even a field name, the masked form shows first-4/last-4 for secrets while non-secrets render in full, and `saveSettings` rejects a wrong token before ever calling `setProperties`, writes only the one populated field on a real save, and preserves both a blank field's existing value and `ADMIN_SETTINGS_TOKEN` itself untouched (`deleteAllOthers: false`). Still can't be deployed from here - no Apps Script MCP connector exists (only Google Calendar/Drive), and `clasp push`/`clasp deploy` need interactive Google OAuth this session doesn't have - same unresolved manual blocker as every other `gas/` change.
- [2026-08-12]: First of three value-prop-strengthening features worked through in order this session (after the Hub Health panel). The settings page (above) could view/rotate `GLOBAL_GITHUB_TOKEN`/`AI_API_KEY` but never validated either - exactly the class of gap that let the real `GLOBAL_GITHUB_TOKEN` 401 sit undetected through 5 straight `health-report.yml` failures earlier this session, since nothing ever checked the token until something else already needed it and failed. Added `checkConfig(token)` to `gas/settings.js`: on page load (not gated behind a separate button - the whole point is catching this the moment the page opens), it calls `GET /rate_limit` with the current `GLOBAL_GITHUB_TOKEN` and `GET {AI_BASE_URL}/models` with the current `AI_API_KEY` (same URL-join convention already used for the real AI call in `autonomous_agent.js` - plain `` `${baseUrl}/models` `` concatenation, no `URL()` joining), and shows a green "✓ valid" or red "✗ <reason>" next to each field. `checkConfig` re-validates the admin token first, exactly like `saveSettings` already does - `google.script.run` exposes every top-level function in the project to anyone who can reach the deployed URL regardless of token, so skipping that check would make the diagnostic itself an unauthenticated way to probe whether credentials are configured. An unset credential short-circuits to "not configured" without any network call at all, distinguishing "never set" from "set but wrong." Verified via the existing `dev-test-gas-settings.mjs` harness, extended with a fake `UrlFetchApp` (the existing harness had none - this is the first thing in `gas/settings.js` that calls out to a network): a wrong admin token rejects `checkConfig` before any fetch happens; both checks report `ok:true` against mocked 200s; either returns a real "401" detail against a mocked 401; and an unset credential reports "not configured" with zero fetch calls for that one specifically.
- [2026-08-12]: Second of three value-prop-strengthening features worked through in order this session. No path existed for a spoke maintainer to tell the system "this finding was wrong" in a way the AI would ever see again - closing the issue was the closest thing, and nothing reads that. GitHub already attaches a `reactions` summary object (`{"+1", "-1", laugh, ...}`) to every issue `issues.listForRepo` returns - no extra API call needed - so a 👎 on a hub-filed issue was a free signal, just never collected. Added `scripts/collect-issue-feedback.js` (new weekly workflow, `GLOBAL_GITHUB_TOKEN` only, no AI - same shape as `prune-logs.js`/`health-report.js`): for each spoke, lists `cto-hub-auto`-labeled issues, matches each back to its decision-log entry by exact `issueUrl === issue.html_url` string equality (both come from the same GitHub field, no ID-parsing needed), and attaches `feedback: { thumbsUp, thumbsDown, checkedAt }` to any entry with a real thumbs-down count - additive/optional, no existing reader breaks. Copied `prune-logs.js`'s exact read-modify-write-with-retry shape (re-read the live log fresh on every attempt, in case a concurrent heartbeat run appended a new decision) rather than reinventing it - that logic is already correct and tested. Writes nothing when a run finds no new reactions. Fed into **both** Recursive Learning Loop backends, kept in sync per established practice: `api/recursive_learning.js` and `gas/recursive_learning.js` now compute a per-spoke feedback summary from `recentDecisions` and inject a `MAINTAINER FEEDBACK:` line into the per-spoke prompt section, plus one added sentence telling the model to weigh a feedback pattern recurring across spokes as real evidence a check should be adjusted or suppressed, not just repeated. Verified via a new `scripts/dev-test-collect-issue-feedback.mjs` (same `makeFakeOctokit`/`calls` convention as `dev-test-prune-logs.mjs`, reusing its exact retry-failure-count technique): an issue with negative reactions gets feedback attached; one with none is left untouched with zero writes; an entry with no `issueUrl` (never created) is skipped entirely; an entry whose feedback counts haven't changed triggers no write; the retry-on-409 path succeeds on a later attempt; and it gives up after exhausting retries. Extended both recursive-learning harnesses (`dev-test-recursive-learning.mjs`/`dev-test-gas-recursive-learning.mjs`) with a fake that now captures the actual prompt sent to the AI (previously discarded), asserting the real feedback count appears in it when present, and "none" when it doesn't. Also re-synced `setup_hub.py`'s embedded copies - `api/recursive_learning.js`'s prompt-building edit above drifted the installer's copy the moment it landed, caught immediately by running the installer-drift check locally before committing rather than after CI flagged it; added `scripts/collect-issue-feedback.js`/`.github/workflows/collect-issue-feedback.yml` to the installer's generated file set too (same class of gap PR #13 fixed once already - a new capability the installer wouldn't otherwise produce for a fresh hub) and extended `ci.yml`'s drift-check file list to cover both new files going forward.
- [2026-08-12]: Third and last of three value-prop-strengthening features worked through in order this session. Added `scripts/doctor.js` - a pre-flight check covering exactly the two failure classes already confirmed real earlier this session: an invalid `GLOBAL_GITHUB_TOKEN` (`GET /rate_limit`) and a spoke with no `VERCEL_URL`/`APPS_SCRIPT_URL` secret set at all (checked via `GET /repos/{owner}/{repo}/actions/secrets` - names only, GitHub never exposes secret values, disclosed plainly rather than overclaiming this would have caught every variant of the real incident, only the fully-unset one). Deliberately `workflow_dispatch`-only, no schedule - this whole investigation started because scheduled workflows were failing silently with nobody watching, so adding another scheduled job here would risk the identical failure mode this tool exists to catch. Two real bugs found and fixed *while building this*, both by actually running the CLI against the real (unauthenticated) API rather than trusting the mock harness alone: (1) `GET /rate_limit` itself returns 200 for a fully unauthenticated request - an empty `GLOBAL_GITHUB_TOKEN` was silently reporting "valid" until the token's presence is checked directly first, the same class of fix `AI_API_KEY`'s check already had; (2) the `call-hub.yml`-exists check was reporting *any* error (including a rate-limit hit encountered live while testing) as "not found - spoke not wired up," conflating two completely different failure modes - fixed by checking the error's actual status code and only using that specific message for a real 404. Verified via `scripts/dev-test-doctor.mjs` (18 checks, same `makeFakeOctokit`/DI convention as every other script): all-healthy passes; a 401 on either credential is caught with a real detail and doesn't stop the remaining checks from running; both unset-credential short-circuits skip the network call entirely; a missing `call-hub.yml`/hub-url-secret is flagged per spoke; a non-404 error checking `call-hub.yml` is now reported distinctly from a genuine miss. Added `scripts/doctor.js`/`.github/workflows/doctor.yml` to `setup_hub.py`'s generated file set (same class of gap PR #13 fixed once already) and `ci.yml`'s drift-check list - caught a real backslash-escaping bug doing this (the installer's Python triple-quoted string needs `\\` doubled for every literal backslash in the embedded JS, e.g. `.join('\n')`/`can\'t`, or Python's own string parser silently corrupts them at the installer's runtime) by actually running the installer into a scratch dir and diffing the output before committing, not just assuming the mechanical insertion was correct.
- [2026-08-13]: Asked whether "Claude Cowork" could provide inference to close the loop to a functional state. Corrected a real misconception first: Cowork is a collaborative Claude workspace product, not a hosted OpenAI-compatible `/chat/completions` endpoint - `autonomous_agent.js`'s existing `fetchImpl(AI_BASE_URL, ...)` call can't just be pointed at it. But the real idea underneath was sound and worth building: the invoking Claude session already has real, working GitHub read/write access to every attached repo (proven repeatedly this whole session) - so instead of waiting indefinitely on a live Vercel/Apps Script deployment plus a configured `AI_API_KEY` (both unresolved since day one), the session itself can perform the debug/hunt/refactor review directly, on demand, reusing every rail `processRequest` already has (decision-log dedup, dry-run default, rate cap, decision logging) unchanged. Built as a new skill, `.claude/skills/mothership-live-review/SKILL.md`, documenting the exact procedure (same fetch order, same prompt structure, same all-or-nothing local-context quirk, same JSON verdict schema) so it's portable to any Claude session with GitHub access to a spoke, not just this one. Deliberately **on-demand only, no schedule** - this project's two worst incidents (the ~1,974-issue hallucination spree, and `thinkos-server`/`tais` silently failing 100+/135+ scheduled runs on an unset secret) were both consequences of *unattended, scheduled* automation; wiring this into a Routine is a separate decision for later, not implied by building it. It also **never files a real issue on its own** - a finding is always reported dry-run, and actually calling `issues.create` needs a separate, explicit go-ahead per finding. Verified end-to-end for real, not just described: ran it against `tso`'s real commit `d020c799cae4162f5eeb03edb7d23caba30aed45` (the hub-URL-secret fix) - found, genuinely from the diff alone, that the new `curl` call has no guard confirming `VERCEL_URL` is actually set before using it, which is *exactly* the root cause independently confirmed later on `thinkos-server`/`tais`'s real, unrelated failures - a real, corroborated finding, not a fabricated one. Reported the dry-run result and appended a real `dry_run_would_create` entry to `tso`'s `ai_decision_log.json` (commit `a29cc68687b10deee1e1ae1c20873732b8584005`) - the first real decision-log write this whole project has ever produced outside a test harness. `api/autonomous_agent.js`/`gas/autonomous_agent.js` and the Recursive Learning Loop's own on-demand extension are both explicitly left untouched/unbuilt for now - flagged in the skill file as deliberate scope boundaries, not oversights.
- [2026-08-13]: Asked to add the *option* for multi-tenancy, billing, and customer isolation - not a full product build, confirmed via four scope-defining questions up front (per-tenant GitHub credential vs. one shared token; hub-provided AI capability metered per tenant vs. bring-your-own-key; usage-metering hooks only vs. a real payment processor; architecture-and-data-model-only vs. real self-service onboarding). Audited the actual current shape first rather than assuming: one hub-wide `GLOBAL_GITHUB_TOKEN` reused identically for every spoke, `spokes.json` a flat array with zero tenant concept, and - a real, previously-undisclosed gap - spoke-to-hub POSTs carried **zero caller credential at all**, so nothing today stopped one caller from triggering a review for any registered owner/repo. Built: new `tenants.json` (hub root, one entry per tenant - `{tenantId, name, status, plan, quota, githubCredentialRef, callerKeyRef?, createdAt}`); `spokes.json` gained a `tenantId` per entry (every pre-existing spoke migrated to `tenantId: "default"`, which resolves to the same `GLOBAL_GITHUB_TOKEN` as before - zero behavior change for the setup that existed before this feature). Credentials/caller keys use a `scheme:value` pointer format (`env:VAR_NAME` reads a real env var/Script Property; `kv:path` is a **disclosed, deliberately-unbuilt placeholder** for a real secrets store that doesn't exist yet - always resolves to `null` today). **Never commit a raw per-tenant credential into `tenants.json`** - stated explicitly since that would permanently leak it into git history and defeat the entire point of the feature. Threaded an `octokitFactory(token)`/`githubFactory(token)` pattern through `api/autonomous_agent.js`, `gas/autonomous_agent.js`, `api/recursive_learning.js`, `gas/recursive_learning.js`, `scripts/doctor.js`, `scripts/health-report.js`, `scripts/prune-logs.js`, and `scripts/collect-issue-feedback.js` - every one resolves a spoke's own tenant credential when the factory is supplied, falling back to the single previously-injected client when it isn't, so every existing caller/test kept working unchanged (verified, not assumed - every pre-existing test in every touched file still passes). Added a separate `hubOctokit`/`hubGithub` - a tenant's own credential has no access to the hub repo itself, so anything that reads/writes the hub's own registries or usage logs needs the hub's credential specifically, never a resolved tenant one. Closed the caller-credential gap: an opt-in `callerKey` request field, checked only if the resolved tenant has a `callerKeyRef` configured - a tenant with none (true for `"default"` today) accepts any/no key, so today's exact zero-auth behavior is unchanged; `setup_spoke.py`'s generated `call-hub.yml` now forwards an optional `TENANT_CALLER_KEY` secret as `callerKey`, unset by default. Added usage-metering hooks (`{tenantId, timestamp, eventType, mode, owner, repo}`, written to a new per-tenant `usage/{tenantId}.json` in the *hub* repo via the same read-modify-write-with-retry convention as `ai_decision_log.json`) firing right after a real AI call resolves and right after an issue is actually filed - explicitly disclosed as "hooks exist and are provably wired up," not a real billing pipeline, since per-tenant JSON files via the GitHub API don't scale or query well for that. Layered a per-tenant monthly quota gate *before* the AI call (not just before filing, like the pre-existing rate cap) since the AI call is the actual cost-incurring event being metered; `quota.reviewsPerMonth: null` (the `"default"` tenant's seeded value) means unlimited, never checked. Redesigned the Recursive Learning Loop to group spokes by `tenantId` and run one fully independent pass per tenant, never pooling two tenants' spoke data in the same AI prompt - verified directly by a test asserting one tenant's lesson text never appears in another tenant's built prompt, in either direction; each PR it opens now explicitly names which tenant's data prompted it, so the human merging it can judge whether generalizing a tenant-specific pattern into the shared standard is appropriate. Updated `.claude/skills/mothership-live-review/SKILL.md` for tenant/usage-hook parity with the code path it mirrors - explicitly disclosed there that the skill still uses whatever GitHub access the invoking session already has rather than resolving a per-tenant credential, since it's a human/session-invoked on-demand procedure, not a multi-tenant HTTP endpoint that needs to keep separate callers apart. Re-synced `setup_hub.py`'s embedded copies of every touched `api/`/`scripts/` file (same backslash-escaping discipline as every prior re-sync) and added `tenants.json` (seeded `[]`) to its generated file set, confirmed via the usual scratch-dir drift check. Verification: extended every touched file's test harness with a two-fake-tenant fixture proving isolation (a tenant's own resolved credential is used, the hub credential/other tenant's spoke is never touched) - `dev-test-handler.mjs`, `dev-test-gas-handler.mjs`, `dev-test-recursive-learning.mjs`, `dev-test-gas-recursive-learning.mjs`, `dev-test-doctor.mjs`, `dev-test-health-report.mjs`, `dev-test-prune-logs.mjs`, `dev-test-collect-issue-feedback.mjs` - full `npm test` sweep green, `python3 -m py_compile` on both installers green. Explicitly out of scope this pass, same as the plan going in: no real secrets store, no Stripe/payment processor, no tenant self-service onboarding UI, no GitHub App installation flow (per-tenant PAT via `env:` remains the interim mechanism), and `setup_hub.py`/`dashboard/` still assume one hub, one operator - this is architecture and data model only, built so real multi-tenancy is possible later without a rewrite, not a finished product today.
- [2026-08-14]: Follow-up to the multi-tenancy pass: asked to let the Recursive Learning Loop learn from *other* repos too - "an organization of builders that pool their bugs and errors should all benefit from cleaner coding overall" - without co-mingling/hallucinating codebase. The per-tenant loop built in the prior pass deliberately never pools two tenants' data, which is correct as a privacy floor but also meant a pattern that only shows up *across* different customers' repos (not within one customer's own multiple spokes) could never surface. A real correction reshaped the design mid-review: the sharing decision belongs to the **repo**, not the tenant - a tenant is a billing/credential boundary, but whether a codebase's bug patterns are safe to pool into a wider corpus is a property of that codebase (one tenant could easily own both a repo they'd contribute from and one they wouldn't; a tenant-level toggle can't express that). Built accordingly: a new optional `shareLearnings: true` field on a `spokes.json` entry (default false/absent, no migration needed) is the sole opt-in unit, completely independent of `tenantId`. Grounded first, not assumed: confirmed directly against the real code that `runForTenant`/`runForTenantRL` never fetch raw diffs or source - only `lessons.md` text and decision-log summaries - so "don't co-mingle codebase" was already structurally true; the actual risks were a real org/repo name ending up quoted in the merged global-standards text, and the model self-reporting an ungrounded "cross-organization pattern." Added `runForSharedPool`/`runForSharedPoolRL`: pools every opted-in spoke across *all* tenants (still resolving each one's data with its own tenant's credential), labels each one `Contributor N` inside the prompt instead of its real name, and requires the model to cite which anonymized contributors its proposal is evidenced by (`supporting_contributors`) - the citation is then verified in **code**, never trusted at face value: counts the distinct real tenants/repos behind the cited labels and only accepts the proposal if it clears a locked bar (2 distinct tenants, or 3 distinct repos within one tenant if it hasn't crossed a tenant boundary) - same "validate the claim, don't trust the free-text self-report" discipline already applied to `has_findings`/`has_proposal` everywhere else, aimed here specifically at the hallucination risk. A repo that opts in still also runs in its own tenant's private pass, unaffected - purely additive, confirmed both decisions (runs-in-both, and the 2-tenant/3-repo evidence bar) with the user directly rather than assuming. The PR a successful shared-pool proposal opens still names the real contributing repos/tenants in its body (built from the code-side label map, never model output) - the anonymization boundary is the model's own reasoning, not the human reviewer, since the hub operator already has full visibility into `spokes.json`/`tenants.json` and hiding names from them would only make the PR harder to sanity-check, not add privacy. Verified with 8 new tests in each of `dev-test-recursive-learning.mjs`/`dev-test-gas-recursive-learning.mjs` (18/19 total per file): an opted-out spoke never appears in the shared-pool prompt or counts toward its evidence; both evidence-bar-clearing shapes (cross-tenant, same-tenant-3-repos) are accepted; a same-tenant pool below both bars is skipped *before* ever calling the AI; the critical anti-hallucination case - the model claims `has_proposal: true` and cites real labels, but those specific labels span only 1 tenant/2 repos despite a bigger pool being available - is rejected by the code-side check regardless of the model's claim; the shared-pool prompt uses only anonymized labels; an accepted live proposal's PR names real repos/tenants; and an opted-in spoke's own tenant-scoped pass still runs unaffected alongside the shared pool. Built as a branch stacked on the still-open `multi-tenant-architecture` (PR #29), since it depends on that branch's tenant data model (`tenantId`, `tenants.json`, `findTenant`/`resolveSecretRef`) which doesn't exist on `main` yet - same stacking convention already used once this session for PR #24 on PR #22's branch.

- [2026-08-24]: Registered `kos`, `argoloth`, and this hub repo itself (`Mothership`) as spokes in `spokes.json`, alongside the existing `tso`/`thinkos-server`/`tais` entries — the first time the hub has monitored a repo whose own commits it's built from. Deliberately did not deploy a live Vercel backend or add scheduled `call-hub.yml` workflows to these three: this project's own worst incident (~2,000 hallucinated issues) and second-worst (thinkos-server/tais spamming 100+/135+ failed scheduled runs on an unset secret) were both consequences of unattended, scheduled automation running with no one watching it. Ran real debug reviews against all four registered non-fork spokes (kos, argoloth, Mothership, tso) instead using the on-demand `mothership-live-review` skill — same prompt shape, same dedup/dry-run/rate-cap/quota rails as `processRequest`, with this session standing in for the AI-backend call. Also found that `setup_spoke.py`'s generated `call-hub.yml` template still emits the pre-fix shape (every-10-minutes-equivalent schedule, zero diff/code context in the payload) — the fix applied directly to `tso`/`thinkos-server`/`tais`'s own workflow files after the incident was never back-ported to this generator, so running it today on a new spoke would silently reintroduce the exact bug already fixed three times elsewhere. Flagged, not yet fixed.

- [2026-08-24]: Fixed the two hub-side bugs found via the on-demand `mothership-live-review` spoke reviews (issues #34/#35), in both `api/autonomous_agent.js` and its Apps Script twin `gas/autonomous_agent.js` (kept in sync deliberately - a fix in one without the other is the exact two-copies drift this file already warns about). (a) The diff builder now orders code files before doc/config-like files (`.md`/`.json`/`.yml`/`.yaml`/`.txt`) and caps each file's own patch at 2000 chars before the overall `MAX_DIFF_CHARS` truncation runs, instead of taking whatever order the commits API returns and cutting wherever the 12,000-char budget happens to run out - a real 122K-char diff had been cut off inside a data-seed file while every file with an actual logic bug never reached the prompt at all. Files that still can't fit are named in an omission note rather than silently dropped. (b) A commit authored by `github-actions[bot]`/`dependabot[bot]`, or touching only `exports/`-prefixed paths, is now skipped before the AI call - a weekly bot-authored issue-export dump was burning a review on unreviewable data every time it ran. (c) `lessons.md` and `NORTH_STAR.md` are now fetched independently rather than in one try/catch - a spoke with a real, current `lessons.md` but a `NORTH_STAR.md` that moved off-root no longer loses both. Also fixed `setup_spoke.py`'s generated `call-hub.yml`, which still emitted the pre-incident every-30-minutes-equivalent schedule with no diff context, silently reintroducing the exact shape that caused the original ~1,974-issue hallucination if ever run on a new spoke - it now generates the same `workflow_dispatch`-only template (with its own explanatory header comment) already proven on `tso`/`thinkos-server`/`tais` since that incident. Added 16 new tests (8 per handler) plus verified the generated workflow YAML parses; full suite (`npm test`) still green.

- [2026-08-24]: Ran hunt and refactor reviews (via mothership-live-review) against kos/argoloth/tso/Mothership's same commits already debug-reviewed, this time using the just-fixed diff builder. Confirms the #34/#35 fixes work as intended: Mothership's own PR #30 merge now gets all 5 real code files into the prompt (previously 1, cut mid-function) with only 2 pure-doc files omitted; tso's bot-authored export commit is now mechanically skipped in every mode, no AI call spent on it. Argoloth's PR #5 merge improved (9 of 26 files now reach the prompt, code-first) but still can't fit everything on a diff this large - Player_Onboarding.gs, Session_Engine.gs, WebApp.gs, World_Clock.gs, and extension/ still don't make it in even post-fix. Worth knowing, not filed as a new issue - MAX_DIFF_CHARS has real cost implications, raising it is a separate, deliberate call. One near-miss avoided by checking history instead of assuming: setup_hub.py embeds a full copy of api/recursive_learning.js's source, which looked like a plausible "de-dup this" refactor finding - checked git log first and found this exact drift was already caught and fixed one commit earlier by an existing, proven installer-drift check in CI. No findings filed from either mode; all decisions logged for real dedup going forward.

- [2026-08-24]: Built the scheduled-job watchdog this repo's own "watch the watchers" lesson had flagged as still open even on itself — `scripts/watchdog.js` + `.github/workflows/watchdog.yml`, weekly (Tuesday 07:00 UTC, after health-report.yml's Monday run) + `workflow_dispatch`. Two checks, one pinned issue updated in place (same pattern as `publishReport`): actionlint against every `.github/workflows/*.yml` file (catches both plain YAML errors and GitHub-Actions-expression-context errors a generic parser would miss), and the most recent run's conclusion for every workflow with a `schedule` trigger (`collect-issue-feedback.yml`, `health-report.yml`, `prune-logs.yml`, `recursive-learning.yml`, `self-reflect.yml`) — judged only by conclusion, never by how long ago it ran, so `recursive-learning.yml`'s monthly cadence never reads as "overdue." 20 new tests, `actionlint` itself confirms the new workflow file is valid. Built for KOS in the same pass (its own `codeql.yml` schedule).
